# 耳畔使用说明

**耳畔，在手机上和 AI 语音通话，不占住你的屏幕。**

玩游戏、看小说、刷小红书时，也能在后台与 AI 连续语音对话，无需来回切换聊天页面。回复边生成边语音，想插话时直接开口，也可以通过悬浮球开关麦克风。

## 也能用于其他开源 AI 聊天前端

**耳畔的语音通话能力可供其他开源 AI 聊天前端复用，包括自己搭建的聊天页面。当前提供现成的 Operit 适配。**

连接其他前端时，开发者把“发送用户消息”和“接收 AI 回复”接到耳畔，就能复用收音、语音识别、朗读和后台通话。聊天对象、角色和历史仍由原来的前端管理。这一层可以通过插件、接口或修改前端源码完成。

前端支持逐步返回文字，就能边生成边语音；返回完整回复时，则在收到后开始朗读。若同时提供停止生成接口，开口打断时还可以取消原来的回复任务。

## 让对话接得更自然

- **流式语音：文字边来，声音边播。** AI 开始回复后，耳畔陆续把文字送去合成；前面的音频一到就开始播放，后面的文字和声音继续生成。
- **双向流式语音：边吐字、边合成、边播放。** 从聊天模型的首字开始接收回复，后续文字持续送入语音合成，音频陆续到达并播放。百炼和 MiniMax 的双向流式接法，在同一个合成任务中持续追加后文，减少反复启动合成带来的段间停顿。
- **自然插话：你开口，它就停下来听。** 耳畔把人声检测、停止朗读和接收下一句话连在一起，让你随时接话。
- **声音线索：可选的情绪信息传递。** 默认只发送你说的话。开发者接入情绪识别后，可以在源码中手动开启声音线索，让识别服务返回的情绪等信息随文字交给聊天模型。

**速度参考：**使用耳畔同一套百炼语音代码的一次电脑联网测试中，从提交朗读文字到收到第一块音频约 **1.25 秒**；同次短句语音识别约 **0.58 秒**。

“聊天首字出现到手机出声”还包含首段文字整理、网络传输和手机播放，这一整段暂未单独计时。日常等待也受聊天模型的思考速度影响；流式让已经生成的回复尽早说出来。

## 可以用哪些模型？

**可以连接云端服务，也支持接入自己部署的本地模型服务。**

- **聊天模型**：在 Operit 中配置。云端模型和本地模型，都沿用 Operit 已接通的聊天设置。
- **把你说的话转成文字**：在耳畔中选择语音识别服务，目前可选阿里云百炼或“Audio API 兼容”。
- **把回复念出来**：在耳畔中选择语音合成服务，目前可选阿里云百炼、MiniMax 官方或“Audio API 兼容”。

自己在电脑或服务器上部署语音模型时，选择“Audio API 兼容”，填写该服务的地址、密钥和模型名。服务需要提供耳畔兼容的语音接口；本地模型在部署它的设备上运行，手机通过网络使用。首次接入请先用一句短话确认识别和播放。

只使用云端服务时，日常通话用手机即可；使用本地服务时，让运行模型的设备保持开机并能被手机访问。

## 语音服务推荐

**阿里云百炼的免费额度**

百炼为部分模型提供免费体验额度。例如北京地域的多个 CosyVoice 语音合成模型各有 **1 万字符的新人免费额度**，可以根据音色、效果和价格自行选择。

具体额度和使用规则请查看[官方说明](https://help.aliyun.com/zh/model-studio/new-free-quota)。

## 当前测试版

**0.3.6：用户已在自己的手机上确认本轮通话流程可用，包括游戏静音后的后台通话；仍有偶发卡顿。**

玩游戏时，先关闭游戏内的音乐、音效和语音。想减少误打断，可以关闭耳畔的“人声打断”，等 AI 这一轮说完再接话。

## 第一次使用

1. **安装耳畔和 Operit。** 先在 Operit 中配置聊天模型，发一句文字，确认能收到回复。
2. **导入连接插件。** 在 Operit 中导入随包的 `phone10-mobile-voice.js`，启用，并运行一次 `setup`（初始化）。
3. **选择聊天对象。** 打开耳畔，进入“连接配置”，点击“选中聊天窗口”，选择你要通话的聊天。
4. **配置声音。** 分别填写“语音识别”和“语音合成”，保存后返回首页。
5. **开始通话。** 点击“开始通话”，首次使用时按手机提示允许麦克风权限，然后直接说话。

已经连通过的用户，更新时覆盖安装耳畔即可，现有配置和已启用的连接插件继续使用。

## 配置里的框框填什么？

- **服务商**：选择由谁识别或合成声音。
- **地址**：填写服务商提供的语音接口地址。选择百炼或 MiniMax 时可从默认地址开始，并确认与账号地域一致。
- **Key（接口密钥）**：从对应服务商的控制台复制，用来验证账号和计算用量。
- **模型**：填写该语音服务支持的模型名称，可先使用默认值。
- **音色 ID（音色编号）**：从合成服务商那里复制你要使用的音色编号。
- **音色名称**：给这个声音起一个自己看得懂的名字，首页会显示它。

识别与合成可以选不同服务商，各自填写对应的密钥。例如识别用百炼、合成用 MiniMax，就分别填写这两家的密钥。两项都选百炼时，可以用页面中的按钮复用识别地址和密钥。

“Audio API 兼容”表示按约定接口连接语音服务，云端和自己部署的服务都可以采用这种接法。让服务提供者给你可从手机访问的 HTTPS 地址、密钥和模型名，分别填入相应位置。本地服务的接口要求见 [开发接入](开发接入.md)。

想换声音时，点击首页的当前音色，进入设置更换音色编号。可以先在服务商的网站挑好音色，再复制过来。

## 通话时怎么操作？

- **开关麦克风**：首页麦克风亮起表示开麦，熄灭表示关麦。
- **人声打断**：在“通话设置”中开关，通话中也能修改。开启时，直接开口即可打断 AI；关闭时，AI 思考和播放期间的声音会被忽略，等这一轮结束后再说。选择会保存在手机中，默认开启。手动关麦和结束语音仍然有效。
- **后台聊天**：开始通话后，直接切到游戏、小说或其他应用。玩游戏时，先在游戏设置中关闭音乐、音效和语音，保留耳畔的通话音量。游戏切换声音焦点时，耳畔保留通话与悬浮球，并在出声时申请播放焦点；建议先用游戏静音的方式。
- **悬浮球**：打开“后台悬浮球”，按系统提示允许耳畔“显示在其他应用上层”。小球会在通话时出现，可以拖动，点击即可开关麦克风，状态与首页同步。
- **更换头像**：点击首页头像，选择本机图片。首页和悬浮球使用同一张头像。
- **结束通话**：点击“结束语音”，停止收音和播放，悬浮球也会收起。

调整声音和连接配置前，先结束通话，再修改并保存。

## “智能结束判断”怎么选？

**想要回复快，保持关闭即可。** 关闭时，停顿约 0.55 秒就开始处理这句话，接着等待识别和 AI 回复。

如果你说话时常常停一下再接着说，可以尝试开启。它会请一个文本模型判断这句话是否说完，适当多等一会儿。

开启前需要填写判断服务的地址、密钥和模型。这会增加一次文本模型判断的用量，也可能让等待变长。按自己的说话习惯选择即可。

## 费用和隐私

聊天、语音识别、语音合成，以及开启后的智能判断，分别使用你配置的服务，由对应服务商计费。具体价格和免费额度可以在服务商控制台查看。

自己部署的本地模型使用自己的设备算力；通过收费平台托管时，按该平台的规则计费。

耳畔把录音交给识别服务，把要朗读的文字交给合成服务。密钥加密保存在手机中，聊天记录由 Operit 管理。声音线索默认关闭；开发者在源码中开启后，识别服务返回的情绪、停顿和时间等线索才会随这句话交给聊天模型。

## 遇到问题时

- **找不到聊天窗口**：打开 Operit，确认连接插件已启用并完成初始化，再回耳畔选择。
- **说话后没有回复**：先看耳畔首页的当前状态，再确认 Operit 的文字聊天是否正常、识别服务的配置是否正确。
- **有文字但没声音**：检查媒体音量、蓝牙声音输出，以及合成服务的密钥、模型和音色编号。
- **切到后台后断开**：在手机系统设置里允许耳畔后台运行，关闭针对耳畔的省电限制。
- **悬浮球没有出现**：确认正在通话，已打开“后台悬浮球”，并给耳畔授予悬浮显示权限。

## 后续想做什么？

- **手机也能用本地语音模型**：计划开源 Qwen3-TTS 的双向流式接法。模型在电脑上运行，手机通过耳畔接入，文字持续输入，声音边合成边传回手机播放。
- **会动的头像**：让首页头像和悬浮球动起来，随倾听、思考、说话切换状态，让通话更有陪伴感。
- **游戏联动**：自用环境已实现，让 AI 结合游戏画面和当前发生的事，陪你边玩边聊。后续根据需求整理开源。
- **探索官方闭源 App 适配**：尝试通过安卓无障碍连接官方闭源聊天应用。

后续进展会随版本更新说明。

## 关于耳畔

耳畔开发了手机上的完整连续通话流程，包括后台收音、自动提交、回复流式语音、开口打断、聊天连接、服务商接入，以及首页与悬浮球的联动。

工程基础参考了 LiveKit Android 示例，人声检测使用 Silero VAD。聊天连接目前提供 Operit 适配，自用环境已跑通 Codex 的双向流式语音连接。相关开源项目与依赖的署名、许可保留在“许可说明”中。
