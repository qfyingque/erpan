# 耳畔 · erpan voice

耳畔在 Android 手机上运行，通过用户配置的语音服务完成识别与合成，通过 Operit 插件连接聊天。

中文配置步骤请先看 [使用说明](使用说明.md)。耳畔提供首页、连接配置和关于页，支持独立选择识别与合成服务、MiniMax 官方双向流式、悬浮头像和可选语义结束判断。0.3.6 新增人声打断开关，并处理游戏静音场景的音频焦点切换。

当前版本为 **0.3.6 开发测试版**。构建、协议测试与真实手机连麦分开验收，见 [验证与发布状态](VERIFICATION.md)；数据流向见 [隐私说明](PRIVACY.md)，目录和贡献说明见 [开发说明](CONTRIBUTING.md)。

## 面向开源 AI 聊天前端的适配

耳畔提供可供开源 AI 聊天前端复用的手机语音通话能力：连续收音、自动提交、流式朗读、开口打断和后台控制。**当前提供并验证的连接是 Operit；自建及其他开源 AI 聊天前端可基于这套语音能力开发各自的连接适配。**

开源聊天前端本身已有发送消息和接收回复的内部流程。适配者可以通过插件、已有 API 或修改源码，将耳畔连接到这些流程，不要求前端事先暴露专用的外部接口。会话、角色、历史和工具继续由聊天前端管理。

- **基本连麦**需要把识别文字送进目标会话，并取得对应回复。
- **增量回复**可以让耳畔边生成边朗读；前端只返回完整回复时，也可以等回复完成后朗读。
- **取消生成**是增强能力，不是接入门槛。前端没有取消接口时，适配层仍可停止本机朗读、丢弃旧回复的后续输出；旧模型请求可能继续运行和计费。前端若不允许同一会话并发发送，下一句需等待旧请求结束，由该前端的适配层处理顺序，不能假定旧回合已取消。

因此，没有停止生成接口也能适配基本语音聊天；能否完整保留快速插话后的连续对话体验，需要结合该前端的会话机制验证。本版连接代码和插件仍针对 Operit，其他前端需要开发连接适配，尚未提供填写任意前端地址即可通用的入口。

## 使用

1. 安装耳畔 APK（原 Phone10 Mobile 独立测试版）。
2. 在 Operit 中导入配套的 `operit/phone10-mobile-voice.js`，启用该工具包，运行它的 `setup`。这只安装本机工作流，不向模型发消息。Operit 必须支持 `Tools.Chat.sendMessageStreaming`。已有可用插件的用户本次只更新 APK，保留插件与工作流。随包插件采用已验证的广播路线，仅补充重复请求不干扰原回复的保护；未启用 worker 候选。
   Operit 的本地导入目前会拒绝同名包覆盖。更新已有插件时，保留旧脚本备份，在包管理中移除旧的 `phone10_mobile_voice` 包，再导入新版并启用；保留已有工作流，不重复运行 `setup`。
3. 先确认 Operit 内的文字聊天可以使用。在耳畔的「连接配置」点「选中聊天窗口」，选你要连麦的那一条。
4. 选择语音服务并填写你自己的地址、API Key、模型和音色，保存。新安装默认「阿里云百炼」，旧版保存的 Audio API 配置继续保留。没有自动提取 Operit 的模型密钥。
5. 点「开始通话」并允许麦克风权限。说完后约 0.55 秒静音提交；百炼合成在一条回复内持续追加文字并播放，整条回复只建立一个合成任务。开启“人声打断”时，再次说话会打断旧回复；关闭时，思考和播放期间的声音会被忽略。首页「麦克风」开关暂停/恢复录音；「结束语音」或通知中的「挂断」结束会话。重新打开 App 不会自动开麦。

建议首次用耳机测试。扬声器回声和后台运行表现与手机系统有关。连续一句超过 30 秒时会关麦并提示分段重说。

## 支持的语音协议

### 阿里云百炼（0.2.0 新增）

- 识别默认 `paraformer-realtime-v2`，合成默认 `cosyvoice-v3.5-plus`。音色 ID 须属于当前账号且匹配合成模型；可填写该模型支持的系统音色或自己的复刻音色。
- 北京公共地址默认 `wss://dashscope.aliyuncs.com/api-ws/v1/inference`。也支持自己的业务空间 WSS 地址；填写 HTTPS 根地址或 `/api/v1` 地址时会转换为 WebSocket 推理地址。不能填写聊天的 `/compatible-mode/v1`。
- Paraformer 当前在北京地域使用。地址、Key、模型和音色必须使用相匹配的地域；相同地址和 Key 可点击「合成使用上面的识别地址和密钥」复用。
- 识别将本机分段的 16kHz 单声道 PCM 发送给百炼，立即发送结束指令，并等待最终识别结果；不额外等待云端默认静音时长。合成按 Operit 的可朗读文字分段，以 24kHz PCM 边接收边播放。
- 使用 Paraformer/CosyVoice 原生任务协议；Qwen-ASR Realtime 等不同协议未适配。模型文字 API 可用不能证明语音模型权限可用。
- 每次识别/每条回复合成拥有自己的任务 ID 与连接；挂断、插话、超时或协议失败会释放连接，不自动重发。识别最多等待 90 秒，连续合成的时限及累计音频上限为 10 分钟，录音段仍最多 30 秒。

### Audio API 兼容接口

- 识别：`POST <base>/audio/transcriptions`，Bearer key，multipart `model`、`response_format=json`、`file`（16kHz 单声道 16bit WAV）；响应 JSON 必须有 `text`。
- 合成：`POST <base>/audio/speech`，Bearer key，JSON `model`、`voice`、`input`、`response_format=pcm`；响应是流式 24kHz 单声道、16bit little-endian 原始 PCM。
- HTTPS；禁止自动跨地址重定向和请求重试。模型 API 兼容 OpenAI 并不代表它也实现语音接口。这个选项不适配只返回 MP3/WAV 或其他采样率的服务。
- 用户的声音只在内存中分段后发送至用户选定的识别服务；可朗读回复发送至合成服务。密钥由 Android Keystore 加密保存，禁止备份；本 App 不持久保存录音和转录。Operit 按自身设置保存聊天。

## Operit 的角色

Operit 是唯一聊天所有者：角色、历史、工具与模型设置保持在 Operit。插件把识别文字交给 Operit 的普通发送入口，并回传同一条回复流；不另开模型客户端，不重复写聊天历史。手机只接收获授权的本机 ContentProvider 事件，不对网络开放服务器。

超时或结果不明确时不会自动重发。Operit 内的一条聊天同一时间只能由一次语音请求占用；手动聊天与连麦应避免同时发送。挂断会立即停本机录音和播放，并尽力取消 Operit 的旧回合。

当前随包插件使用广播路线。此前 worker 候选未通过手机测试，未作为默认发布插件；长时间模型等待可能导致 Operit 卡顿的限制仍保留。

## 编译

解压独立源码包，在包含 `gradlew.bat` 的根目录执行。使用 JDK 21、Android SDK 36 和 Node.js 22 或更新版本（用于插件测试），允许 Gradle 从 Google、Maven Central、JitPack 下载依赖。设置 `JAVA_HOME` 和 `ANDROID_HOME`，或在本机的 `local.properties` 写 `sdk.dir`（不要提交该文件）。Gradle Wrapper 固定为 9.1.0；首次运行需下载构建工具和依赖。

```powershell
.\gradlew.bat :mobilevoice:testDebugUnitTest :mobilevoice:assembleDebug :mobilevoice:lintDebug
node --test tests/mobile-operit.test.mjs
```

APK：`mobilevoice/build/outputs/apk/debug/mobilevoice-debug.apk`。这是 debug 签名的测试包；正式版签名另行配置，签名密钥由维护者自行保管。仓库与 APK 不包含任何可用 API Key。

## 排查

- Operit 列表无响应：打开 Operit，确认工具包启用且 setup 成功；检查工作流仍启用。
- 不要导入实验 worker 脚本；请使用随包的广播脚本。已有可用插件不必为本次 APK 功能更新而重装。
- 提示识别失败：检查识别地址、模型、key 和网络；Operit 不会收到空转录。
- Operit 已有文字但没有声音：检查合成地址、声音名称和是否真正返回指定 PCM 格式。
- 后台中断：确认通知中的连麦服务仍存在，检查系统对两款 App 的后台限制。

语音由 AI 合成。

协议依据：[百炼合成](https://help.aliyun.com/zh/model-studio/cosyvoice-websocket-api)、[合成客户端事件](https://help.aliyun.com/zh/model-studio/cosyvoice-client-events)、[合成服务端事件](https://help.aliyun.com/zh/model-studio/cosyvoice-server-events)、[Paraformer WebSocket](https://help.aliyun.com/zh/model-studio/websocket-for-paraformer-real-time-service)、[识别客户端事件](https://help.aliyun.com/zh/model-studio/paraformer-client-events)、[识别服务端事件](https://help.aliyun.com/zh/model-studio/paraformer-server-events)。
