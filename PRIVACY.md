# 数据与权限

本文对应耳畔独立手机版 0.3.6。Operit、用户选择的模型服务商分别有自己的数据政策。

## 数据流向

| 数据 | 去向与保存方式 |
| --- | --- |
| 麦克风录音 | 内存中分段，发送至用户配置的识别服务；耳畔不写录音文件 |
| 识别文字 | 通过本机授权通道交给 Operit；Operit 按其设置保存聊天 |
| 回复文字 | 从 Operit 返回，发送至用户配置的合成服务 |
| 合成音频 | 内存中接收并播放；耳畔不保存音频文件 |
| 智能结束判断 | 默认关闭；开启后会把识别文字发给另外配置的判断服务 |
| 服务地址、API Key、聊天选择 | 使用 Android Keystore 的 AES-GCM 加密，保存在本应用非备份目录 |
| 自选头像 | 仅读取用户通过系统选择器选中的图片，保存到本应用目录 |

云服务可能保留请求，保留期限由所选服务商决定；本应用不能替服务商承诺不存储。API Key 的本机加密不能抵御已被完全控制的手机。卸载或清除应用数据可能丢失配置，应提前自行保存所需信息。

## 权限用途

- 麦克风：通话收音。
- 网络：调用用户配置的语音和判断接口。
- 前台服务与通知：运行中的语音会话及挂断入口。
- 显示在其他应用上层：用户启用后，通话期间显示悬浮球。
- 音频设置、唤醒锁：管理会话音频及通话运行状态。

独立耳畔应用不申请无障碍权限；本版只通过 Operit 插件接聊天。DeepSeek 等其他应用尚未适配。

## 分享问题报告

请只提供版本、设备系统、步骤和错误阶段。不要公开 API Key、配置文件、私人聊天、私人音色 ID 或含敏感内容的录屏。诊断事件用于描述阶段与相对时间，不应包含对话正文或密钥。

声音线索默认关闭。开发者在源码中手动开启后，才会将识别服务返回的有效时间信息及受支持的情感估计，附在 Operit 的同一条用户消息中。它们由 Operit 和所选聊天服务处理、随聊天保存。本版没有额外采集、推断年龄或口音，也没有增加独立的情绪服务请求。
